import { Resolver } from 'unplugin-auto-import/types';

interface AntdResolverOptions {
    /**
     * 组件前缀
     * @description 例如使用前缀 A，Button 自动导入改为 AButton
     */
    prefix?: string;
    /**
     * 包名
     * @default antd
     */
    packageName?: string;
}

declare const antdResolver: (options?: AntdResolverOptions) => Resolver;

export { antdResolver as default };
